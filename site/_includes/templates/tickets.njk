{% import "components.njk" as comp %}

<section class="ly-section tickets" id="{{ tickets.id }}">
    <header class="tickets-header">
        <h2>{{ tickets.title }}</h2>
    </header>

    {{ tickets.text | md | type | safe }}

    <ul class="tickets-list">
        {% for ticket in tickets.types %}
        <li class="tickets-item">
            <h3>{{ ticket.title }}</h3>

            {% if ticket.subtitle %}
                <small>{{ ticket.subtitle }}</small>
            {% endif %}

            <ul>
            {% for item in ticket.features %}
                <li>
                    {{ item }}
                </li>
            {% endfor %}
            </ul>

            <strong>{{ ticket.price | md | type | safe }}</strong>

            {{ comp.link(ticket.btn) }}
        </li>
        {% endfor %}
    </ul>
</section>
